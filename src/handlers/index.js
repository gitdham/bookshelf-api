const { deleteBookHandler } = require('./deleteBookHandler')
const { getBookHandler } = require('./getBookHandler')
const { postBookHandler } = require('./postBookHandler')
const { putBookHandler } = require('./putBookHandler')
const { showBookHandler } = require('./showBookHandler')

module.exports = {
  deleteBookHandler,
  getBookHandler,
  postBookHandler,
  putBookHandler,
  showBookHandler,
}
